# 共同開発ルール

このリポジトリを扱う人・エージェントは、毎回このファイルを入口として作業する。
会話履歴の自動同期に依存せず、Gitの履歴と共有文書を引き継ぐ。

## 作業開始

1. `hostname`、`pwd`、`git status --short --branch`、`git remote -v` で実行先と変更状態を確認する。
2. `git fetch origin` を実行する。失敗した場合は「最新確認不能」と記録し、同期済みと報告しない。
3. `git log --oneline --left-right HEAD...origin/main` と必要な差分を読む。未コミット・未追跡ファイルを勝手に削除・退避・コミットしない。
4. mainがクリーンでローカル独自コミットがない場合だけ `git merge --ff-only origin/main` で取り込む。分岐や競合があれば両側の変更意図を確認して解決する。強制pushや強制リセットはしない。
5. [現在の状態](docs/STATUS.md)、[構成と前提](docs/PROJECT_CONTEXT.md)、[判断の記録](docs/DECISIONS.md)、[運用手順](docs/OPERATIONS.md) を読む。コード変更時は対象配下の追加AGENTS.mdも確認する。
6. 新しい作業を始める前に担当ホスト・目的・対象範囲を明確にする。原則サブPCで編集・テストし、メインPCは指示とレビューを担当する。この役割はユーザーの明示指示で変更できる。

## 同時作業と共有

- 同じ作業を両PCで同時に編集しない。並行作業が必要なら別ブランチ（例：`work/<topic>`）・別範囲を使う。
- 作業開始と中断時には STATUS.md に担当・ブランチ・次の一手を記録して共有する。これは引継ぎ用であり、Gitによる排他ロックではない。
- 全自動pull、作業ツリーの双方向上書き、無条件の `git add .` は使わない。
- 設計変更は DECISIONS.md、実行方法変更は OPERATIONS.md、完了・未完了は STATUS.md に同じ変更内で反映する。
- 会話で出ただけの案を「実装済み」にしない。コード・テスト・デプロイ・同期・実機動作確認を区別する。
- 共有文書は参照資料。最新のユーザー指示と実コードに照らし、矛盾は明示して修正する。

## 開発の判断基準

- 観測値と推定値、確認済みと仮説、未確認と失敗を区別する。
- バス位置は停留所イベントが基礎。滑らかな表示だけで精度向上と判断せず、累積到着誤差やMAEで評価する。
- 無料枠のCPU・要求数・D1変更行数・R2容量/操作数は別々に扱う。バッチ化だけでD1行数が減ると説明しない。
- 障害時のフォールバックを保つ。点検依頼から再集計・本番変更を無断で推測しない。
- 既存のユーザー変更を保護し、変更範囲に応じて検証する。手順は OPERATIONS.md を参照。

## 作業終了

1. `git diff --check`、対象変更の確認、必要なテストを行う。文書だけの変更ではリンク・事実・機密情報を確認する。
2. STATUS.md に確認日時、根拠、実施した検証、残件と次の一手を記録する。
3. 関連ファイルだけを明示してstageし、`git diff --cached` を確認して意味のある単位でcommitする。
4. ユーザーが共有を許可した作業はpushする。push失敗なら「コミット済み・未共有」と報告する。拒否されたらfetchして他者の変更を確認し、強制pushで解決しない。
5. 受け取る側でfetch・変更保護・fast-forward・コミット一致確認を行う。受け取り未確認なら「相手PC反映未確認」とする。

APIトークン、SSH鍵、メール本文、個人の接続先やアカウント情報を新規にコミットしない。
端末固有情報はGit管理外の `docs/local/` や各端末の秘密情報管理機能へ置く。
`.gitignore` は既に追跡済みの秘密情報を保護しないため、stage時の確認は省略しない。
