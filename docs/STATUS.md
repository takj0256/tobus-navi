# 現在の状態・次の作業

更新日：2026-09-11 JST。ここは運用の引継ぎ。古い [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md) は機能履歴で、現在の稼働証明ではない。

## 2026-09-11 JSON公開失敗の修正・再実行

- 04:15 JSTの初回JSON集計は28日分の取得と269,783 profiles / 1,302 weather profilesの生成まで成功したが、R2へ6並列PUT中に`ECONNRESET`が1回発生し、再試行がなかったため04:25 JSTに失敗した。R2の世代manifestと`profiles-v1/current.json`は未作成で、不完全な世代は公開されていなかった。
- R2通信を3並列へ抑え、ネットワーク例外とHTTP 408/425/429/5xxに最大8回の指数バックオフ再試行、1要求120秒のタイムアウトを追加した。認証エラー等の恒久4xxは再試行せず停止する。生成器のログは巨大な全シャード一覧から集計要約へ縮小した。
- 模擬R2で初回503からの回復、401の即時停止、世代manifest検証後にcurrentを最後に更新する順序を確認。`npm run check:js`、`npm run test:js`（92件成功）、`bash -n`、`git diff --check`も成功した。
- 修正版を集計用コピーへ反映し、旧版を日付付きで退避。手動再実行により28日分（2026-08-14～09-10）、269,783 profiles、1,302 weather profiles、218シャードを221個のR2オブジェクトとして公開完了した。生成時刻は2026-09-11 12:52:33 JST。
- R2からcurrentと世代manifestを再取得して完全一致を確認。抽出シャードとweather JSONもmanifest記載のサイズ・SHA-256と一致した。ロックは解放済みで、D1は旧値268,779 profiles / 1,292 weather profilesのまま、確認照会のrows_writtenは0。
- 本番Workerは引き続きD1参照。今回の成功はJSON生成・R2公開までであり、WorkerのJSON参照切替やデプロイは未実施。

次の一手：2026-09-12 04:15の自動実行でも再試行付き公開が完了することを05:00時報で確認する。Workerの参照切替は別途設計・検証する。

## 2026-09-10 04:15集計のJSON化

- 担当ホスト：サブPC `MSI`。ブランチ：`work/phase11-json-profiles`。
- 2026-09-10 04:15 JSTの旧方式はD1へ335,519行を書き込み、無料枠100,000 rows_writtenの超過通知を再度発生させた。09:00 JSTのリセット後、D1読み取りは成功した。
- 日次スクリプトからD1のrunning/failed更新と全件SQL投入を外し、R2 `profiles-v1/generations/<生成ID>/`へのJSON公開へ変更した。各JSONのサイズ・SHA-256を検証し、世代manifestを再読込した後に`profiles-v1/current.json`を最後に切り替える。
- 検証：`npm run check:js`、`npm run test:js`（92件すべて成功）、`bash -n tools/run_phase11_local_aggregation.sh`、`git diff --check`。模擬R2サーバーでcurrentが最後に書かれることを確認した。
- 集計用コピーへランナー・JSON生成器・R2公開器を反映し、ソースとの一致を`cmp`で確認した。旧ランナーはバッチコピー内に日付付きで退避。04:15 cronは同じランナーパスを参照し、ロックは解放済み。非対話のWranglerでR2 `daily-v2/2026-09-09.json`の読み取りにも成功した。
- 05:00の同名時報は重複なしで、R2 `profiles-v1/current.json`を当日成功の一次根拠にする内容へ更新した。
- 本番Workerの参照先は未変更で、引き続きD1。過去分の再集計、R2への実データ公開、Workerデプロイはこの確認では行っていない。

次の一手：次回04:15実行後にR2 current・ログ・D1書き込み超過の非再発を確認する。Workerの参照切替はキャッシュ・R2読取コスト・D1フォールバックを検証してから別途行う。

## 2026-09-09 サブPC欠損復元・JSON集計

- 担当ホスト：サブPC `MSI`。ブランチ：`work/phase11-json-profiles`。
- 作業開始時のHEADは `a935335`。開始時はWSLのDNS障害で `git fetch origin` が失敗したが、静的DNSとGitHub SSH 443番、このPC専用鍵を設定して接続を回復した。
- `origin/main` の `a935335` を確認済み。JSON集計の `d881843` は `origin/work/phase11-json-profiles` へpushし、ローカルとGitHubのHEAD一致を確認した。
- R2 `daily-v2` の2026-09-03～09-08が欠損していることを実測し、元の `events` / `hourly` から6日分を復元した。各オブジェクトはアップロード後に再読込し、日付と非空グループを確認した。
- 2026-08-12～09-08の28日分を使い、D1書き込みを行わずJSON集計を実施。265,534 profiles、1,257 weather profilesを生成した。
- profile統計：平均信頼度0.497361、最大0.981166、最大sample_count 40。weather profile統計：平均信頼度0.854541、最大0.964082、最大sample_count 793,503。
- 出力は曜日区分・15分枠別の218シャード、合計約101MB。全シャードとweather JSONのSHA-256、件数、JSON構文を検証済み。
- D1は更新していない。D1実値は2026-09-03 04:16 JST生成の255,401 profiles / 1,231 weather profilesのまま。
- 検証：`npm run check:js`、`npm run test:js`（90件すべて成功）、実データJSONの全シャード検証。
- 本番未反映：profile JSONはローカル生成のみ。R2へのprofile JSON公開、Worker参照切替、集計用コピー更新、Workerデプロイは未実施。

次の一手：profile JSONのR2キー、manifestの原子的切替、Workerキャッシュ、D1フォールバック、R2読取回数と遅延を設計・テストした後に公開可否を判断する。

## この変更の引継ぎ

- 目的：両PCで作業前に共通知識とGit履歴を確認する運用を導入。
- 担当ホスト：メインPC（今回の共有文書初期整備）。通常の開発拠点はサブPC。
- ブランチ：main。
- 変更：AGENTS.mdと共有文書4ファイル、README入口、ローカル設定の除外。
- 確認：開始時に両PCとも `5ee0e70` で変更なし。メインでfetch後も同じ。共有文書の反映結果はこの文書を導入するGitコミットと各PCのHEADで確認する。
- 検証：文書リンク・差分・機密内容のチェック。アプリ実装の変更なし。
- 次の担当：サブPCの作業開始時にAGENTS.mdと本ファイルを読み、下記未解決問題の対象を選ぶ。

## 未解決・未確認

| 項目 | 状態と根拠 | 次の一手 |
| --- | --- | --- |
| D1書き込み超過 | 04:15バッチのD1書き込みは削除済み。2026-09-11の修正後再実行でもD1値は不変、確認照会のrows_writtenは0。次回の定刻自動実行は未確認。 | 2026-09-12 04:15後にD1書き込みと上限通知の非再発を確認する。 |
| 日次集計の鮮度 | R2 `profiles-v1/current.json`を2026-09-11 12:52:33 JST生成値へ公開済み。D1と本番Workerは2026-09-10 04:19 JST生成値のまま。 | R2の日次更新を継続確認し、Worker参照切替を別途設計・実施する。 |
| Cloudflare認証 | 2026-09-09、隔離した正常DNS経路ではD1/R2照会とR2復元に成功。通常のWSL resolverは依然タイムアウト。 | WSL DNSを恒久修正し、非対話バッチで再確認する。 |
| SSH/WSL常時稼働 | 09-09点検時はタイムアウト、その後の接続は成功。停止原因は未確定。 | Windows起動・WSL・SSH・スリープを照合。起動しただけで常時稼働完了としない。 |
| 05:00点検のサブPC移設 | サブPC `MSI` に同名重複なしで登録し、Gmail接続を確認済み。当日分は既送信のためテスト送信を抑止。翌日以降の定刻実行は未確認。 | 初回05:00実行後にホスト・点検・送信結果を確認する。メイン側停止は別途切替時に行う。 |
| 開発環境移行 | ソースコピーとSSH接続は確認。開発checkoutとバッチ実行コピーは別。 | 環境・認証・バッチ更新・予定タスクを個別検証。 |
| Work/Android | 会話の引継ぎと全端末アクセスは構想段階。 | 実際の接続・操作を検証してから完了扱いにする。 |
| 精度評価 | 機能実装は存在するが、この文書で本番MAE改善を保証する根拠はない。 | 偏りと季節を含め、実測比較を行う。 |

## 次回更新の型

確認日時（JST）、担当ホスト、作業ブランチ、関連コミット、実施内容、検証コマンドと結果、未確認事項、次の一手を記載する。
自分自身のコミットIDを先に書こうとせず、必要なら前回の関連コミットを記す。
コミット済み・push済み・相手PC取り込み済み・本番反映済みを別々に報告する。
