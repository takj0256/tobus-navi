# Phase 11 テスト結果

実施日：2026-08-04

## JavaScript

```bash
npm run check:js
npm run test:js
```

結果：

- 構文確認：成功
- 単体テスト：63件成功、失敗0件

追加した主な回帰テスト：

- 曜日区分と15分時間帯キー
- 異常値を隔離した週間中央値・信頼度
- 信頼度0.3未満のGTFSフォールバック
- 120秒・300秒・1.5倍の異常境界
- 既存交通キャッシュと複数車両による異常確定
- 後続2区間までの補正減衰と3区間目の完全打切り
- Phase 11バッチAPIのMap変換
- Workerの連続停留所イベント生成と不正遷移除外

- 公開座標を生GPSとして投影しない
- 明示された `STOPPED_AT` だけを停車として扱う
- 連続する停留所イベントから区間所要時間を学習
- 先行車の遅い実績を進行率と到着予測へ反映
- 推定だけでは次停留所を越えず94%で更新待ち
- 区間履歴を14日間の期限付きで保存・復元
- 石島停車中から猿江一丁目までを7分として累積
- 石島、扇橋一丁目、猿江一丁目の到着時刻が順に増加
- 後続区間にも先行車の混雑実績を累積

## Python

```bash
python3 -m py_compile tools/*.py tests/*.py
python3 -m unittest discover -s tests -p "test_*.py" -v
```

結果：

- 構文確認：成功
- 単体テスト：14件成功、失敗0件

## Cloudflare Worker

```bash
cd worker
npx wrangler deploy --dry-run
```

結果：Wrangler 3.114.17でバンドル成功（37.56 KiB、gzip 9.67 KiB）。未設定のD1・R2は本番作成後に`wrangler.toml`へバインドします。本番操作ではNode.js 22以上と最新Wranglerを使用します。

## ローカル配信

```bash
python3 tools/serve.py
curl http://127.0.0.1:8000/
```

結果：成功

## 都営バス公開GTFS-RT実データ

24秒差の2フィードをPhase 10の学習処理へ入力しました。

- 車両数：492台
- `current_status` 明示車両：0台
- 検出した連続停留所区間：122件
- 区間実測時間の中央値：96秒
- 最短実測時間：17秒

公開フィードから車両別の停留所切替を検出し、区間所要時間サンプルを生成できることを確認しました。

## 未確認

- Cloudflare本番環境でのR2・D1・Cron Triggerの連続運転
- 1～2週間収集後のPhase 10対Phase 11のMAE比較
- TomTom実APIキーを使った異常時照会と月間使用量
- Android実機で長時間起動した場合の履歴蓄積量
- 実際のバス到着時刻との長時間比較
- 運休・迂回時の推定位置表示
