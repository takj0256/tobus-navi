# 実装状況 Phase 6

## 完了

- GTFS `parent_station` を使った親停留所単位のグループ化
- 数字だけの子停留所名を「○番のりば」へ変換
- 親停留所名と代表行き先の常時表示
- 複数のりばの折りたたみ表示
- のりばを階層的に見せるツリー風表示
- 同名停留所でも親IDが異なるものは別グループとして維持
- 親IDがない停留所は同名＋500m以内でグループ化
- 時刻表、リアルタイム車両位置、推定到着機能を維持
- 開閉状態をお気に入り更新後も維持

## データ移行

schema version 5へ更新したため、Phase 5以前のGTFS変換結果は再生成が必要。

## 実機確認が必要

- 実際の都バスGTFSで親停留所名が正しく表示されるか
- 駅前ターミナルでのりば数が多い場合の一覧性
- 長い行き先名がAndroid画面幅で読みやすいか
- PWA更新後に古いキャッシュが残らないか
