# 判断の記録

既存会話と2026-09-09時点のコードから整理。提案・採用・実装・検証は区別する。

| ID | 決定・状態 | 理由と制約 |
| --- | --- | --- |
| D01 | 採用：サブPCを主な開発実行拠点にする | メインPC停止中も作業と集計を継続したい。ソースコピーだけでは移行完了ではなく、認証・起動・定期タスクも必要。 |
| D02 | 採用：Gitと共有文書を作業前に読む | 端末間で会話履歴が自動同期される前提を置かない。変更理由と検証結果も残す。 |
| D03 | 実装あり：停留所イベントによる位置・到着推定 | 配信座標を連続GPSとみなす方式は観測と合わなかった。累積到着時間を回帰確認する。 |
| D04 | 実装あり：過去プロファイルを基本とし、異常確定後に交通APIを照会 | 外部APIの常時照会を避ける。気象条件・サンプル偏り・誤差評価も必要。 |
| D05 | 一部実装：重い統計と日次統合をサブPCに移す | Worker CPUの負荷を抑える。時間統合はWorkerに残り、D1書き込み量は別の問題。 |
| D06 | 未実装の改善案：D1差分更新または保存粒度の見直し | 全件投入で無料枠超過の通知あり。差分更新で必ず収まるとは限らず、時刻・重みの変化やインデックス分を含めて実測する。精度維持と原子的な公開も検討する。 |
| D07 | 一部実装：04:15集計、05:00点検・メール | サブPC側の05:00予定タスクとGmail接続は確認済みだが、初回定刻実行は未確認。Gmail接続とSMTP用認証は別物。 |
| D08 | 実装あり：独立地図ページ・道路中心の白地図・枝線合流表示 | 建物情報を減らして経路を見やすくする。実装上の最長経路選択と利用者のいう「本流」が必ず一致するとは限らない。 |
| D09 | D10で採用済み：Phase 11統計を曜日区分・15分枠別JSONシャードとして生成 | D1全件更新を避け、生成単位のmanifestで原子的切替とロールバックを可能にする。Worker参照の実装判断はD11へ継承した。 |
| D10 | 採用：04:15集計の出力先をD1全件UPSERTからR2 `profiles-v1` JSONへ変更 | 2026-09-10の実行で335,519行を書き込み、D1無料枠100,000 rows_writtenを再度超過した。世代別オブジェクトを置き、manifestを検証してから`current.json`を最後に更新する。Workerの参照先変更は別判断とし、D1の既存値をフォールバックとして残す。 |
| D11 | 実装・本番反映済み：WorkerはPhase 11統計をR2 JSON優先、D1フォールバックで参照する | 日次全件D1書き込みを再開せず最新統計を利用する。currentを短時間、immutableな世代シャードを長時間キャッシュし、パス・世代・形式を検証する。R2が正常だが該当シャードに値がない場合は、その世代を正として古いD1値を混ぜない。 |
| D12 | 実装・本番反映済み：遅延は時刻表と到着予測の「幅」を比較して見込み表示する | 予測中心値だけで正常・遅延を断定しない。幅全体が定刻より90秒以上遅いときだけ遅れ、幅全体が90秒以上早いときは定刻前、定刻をまたぐ場合は定刻付近、幅が10分を超えるか位置情報が古い場合は確認中とする。公式運行情報とは表現しない。 |

## 新しい判断の記録方法

### D14（2026-09-18）：長期蓄積を土台に直近・当日補正を維持

ユーザー承認。観測履歴は累積保存し、過去の同季節・類似条件から基礎を作り、直近28日と当日状況で補正する。D1旧集計値は別領域に退避して観測と重複加算しない。ニューラルネットワークは将来の残差学習候補。保存・段階移行を先に運用し、予測切替は時系列評価後とする。実装状況と制約は[長期履歴設計](PHASE11_LONG_HISTORY.md)参照。

### D13（2026-09-15）：WindowsからWSL日次集計を直接起動

採用・サブPC設定済み。03:55:29にWSLが終了し、04:15 cronが起動しなかった事実をjournalで確認。03:55のrefreshスクリプトはスキップされており、同スクリプトが直接shutdownしたという説明は採用しない。cron起動だけでWSLが定刻まで残るという前提を廃し、Windows側の定刻タスクが集計プロセスの終了まで待つ。二重起動防止と成功日ガードを追加し、旧cron/refreshは退避・無効化する。Interactive登録のみ成功したため、ログインなしの実行保証は残課題。定刻・スリープ復帰の検証は手動タスク起動と区別する。

日付、対象、背景、採用案、却下案と理由、検証方法、状態を記す。
撤回した決定は消さず、後継IDを付ける。個人の会話原文やメールを貼り付けず、プロジェクトに必要な要点だけ残す。
